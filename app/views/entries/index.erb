<h2>Welcome, <%= current_user.name%>!</h2>

<% @entries.each do |entry| %>
    <p><%= "#{entry.created_at} | #{entry.glucose} mg/dl | #{entry.carbs}g carbs | #{entry.insulin}u insulin  " %><a href="/entries/<%=entry.id%>">See more</a></p>
<% end %>